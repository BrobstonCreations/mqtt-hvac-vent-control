export default interface Vent {
    commandTopic: string;
    name: string;
    payloadClose: string;
    payloadOpen: string;
    stateClosed: string;
    stateOpen: string;
    stateTopic: string;
}
